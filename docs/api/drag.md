# Drag API (v0.2 Preview)

The DragController coordinates:

- `pointerdown`
- `pointermove`
- `pointerup`

It integrates with:

- snapshot creation
- constraint evaluation
- rollback behavior

This API will expand in v0.2.